# 沉浸式小说背单词：质量改进交接

更新日期：2026-08-25

## 当前结论

本轮已经把“把手一挥 → handle 一挥”从一个孤立特例，提升为可扩展的生产策略：候选词现在分为稳定词、语境词和阻断词；只有词条、边界和上下文都通过，才允许进入替换池。无法确定时保留中文，密度只在安全池中选取。

本轮只修改代码、测试和审计脚本，没有修改本地小说原文。翻译质量策略仍由 `584b36b` 提供；阅读器升级已包含目录跳转、自动翻页、阅读背景和三种翻页方式，发布状态以本文件末尾记录为准。

## 已实现的质量策略

- `src/data/candidate-policy.ts`
  - `stable`：完整词条且已有生产批准。
  - `contextual`：必须命中明确的左/右上下文规则，例如“门把手”“握住把手”“获得自由”。
  - `blocked`：来源词库残片或已确认语义错误，例如 `国际→chess`、`公共→bus`、`给小→tip`、`下定→define`、`周日→weekday`。
- `src/core/tokenizer.ts`
  - 在分词路径和扫描路径都执行同一套策略。
  - 增加复合词保护，避免把“电影院”“自然光线”“信号塔”等切成错误的短词。
  - 每个匹配记录是否有上下文证据。
- `src/core/corrections.ts`
  - 语境词没有命中提示时不能因为词条优先级而被选中。
  - 已批准的单一通用词义可以压过来源词库中的未批准同义词，但不会压过明确语境。
- `src/core/replacer.ts`
  - 生产替换必须是高置信度、非阻断、已批准或明确纠正的候选。
  - 保留每句最多 2 次、每个中文词每章最多 2 次，以及低/中/高密度为最大安全池 `1/3`、`2/3`、`100%`。
- `scripts/audit-production-candidates.mjs`
  - 审计候选重复、阻断原始候选、上下文候选、来源映射和词库残片警告。
  - 当前有效生产候选 1164 个；原始批次中的历史阻断项只保留用于审计，不会进入生产。

## 本轮新增：目录与章节跳转

- 阅读器右上角的“目录”打开可滚动章节面板，当前章节高亮并显示当前阅读百分比；点击任意章节后立即跳转并从该章顶部开始阅读。
- 支持 `第 1 章`、`第一章`、`序章`、`序幕`、`楔子`、`引子`、`尾声`、`后记`、`番外` 等常见标题形式。
- 第一章前的书名/作者/序言内容会并入首章正文，不再丢失，也不会让已有的 `chapterIndex` 进度整体错位。
- 目录面板支持点击遮罩或按 `Escape` 关闭，适配移动端底部安全区。

## 本地验收结果

语料：3468 条 reviewed 标注，按书籍隔离 development / validation / blind；blind 为 2700 条、1481 个期望替换。

| 集合 | 实际替换 | 正确替换 | 端到端精度 | 覆盖率 |
| --- | ---: | ---: | ---: | ---: |
| 全部 reviewed 标注事件 | 1018 | 1013 | 99.509% | 56.814% |
| blind 标注事件 | 833 | 832 | 99.880% | 56.178% |

另外，使用高密度、每句/每章上限完全走一遍 blind 书籍，实际产生 **63,159 次**替换尝试；这项计数与标注事件分开，不能用来抬高精度或覆盖率。

Wilson 95% 区间：全部精度 98.855%–99.790%（1013/1018），blind 精度 99.323%–99.979%（832/833）；全部覆盖率 54.503%–59.096%（1013/1783），blind 覆盖率 53.639%–58.686%（832/1481）。

当前硬门槛写在 `tests/quality/local-novel-evaluation.test.ts`：

- 全部 reviewed：至少 1000 次替换、精度 ≥99.5%、覆盖率 ≥55%。
- blind 标注事件：至少 800 次替换、精度 ≥99.5%、覆盖率 ≥55%。
- 完整 blind 书籍的真实 reader path：至少 1000 次替换尝试（当前 63,159 次）。
- 每个有样本的盲测类型：精度 ≥99%、覆盖率 ≥45%。
- 另外保留词性、分词边界、停留中文的准确率统计，避免只靠一个总数掩盖严重错误。

已通过：

```bash
npm test                         # 101 passed, 1 skipped
npm run build                    # TypeScript + Vite 构建通过
npm run quality:audit-candidates # 候选审计通过
npm run quality:audit-corpus     # 41 个文件；发现 1 对近重复文件，已记录审计结果
npm run quality:novels           # 上述精度/覆盖率硬门槛通过
```

## 仍应继续观察的地方

当前剩余的 5 个 reviewed 误替换集中在 `突然`、`作者`、`电影` 的多义/元数据语境中；它们没有达到“把手一挥”那种明显荒谬程度，但适合继续用真实正反例收紧。下一轮应优先审计：

1. 书名、作者行、章节标题和正文混在一起的元数据。
2. `电影/电视剧/电影院`、`作者/作家` 等可被更长复合词或专名包住的词。
3. 语义正确但用户不希望打断阅读的高频虚词、时间词和描述词。
4. 新发现的词库残片：先加入 `BLOCKED_TERMS`，再补一个最小回归样例；不要直接扩大全局白名单。

## 产品层后续建议

- 先修正 TXT 单换行分段和句子边界，再扩展章节标题识别；否则目录、上下文和质量标注都会受影响。
- 增加“这处替换不对”的局部反馈，区分分词、词义和词性，并按当前语境抑制，而不是全局封禁中文词。
- 章节目录、阅读设置和分页阅读已发布；后续可在真实使用反馈基础上继续优化搜索跳转和桌面端排版。
- 继续保持离线、轻量、数据驱动；不要为解决少量歧义直接引入 BERT/LTP 等重模型。

## 后续产品路线（2026-08-26 更新）

1、2 已合并发布：包含纵向/横向/仿真三种翻页、自动翻页、章节练习续读和十款本地阅读背景；横向/仿真已改为固定页宽显式列布局，设置页是自动翻页、背景和排版的唯一配置入口。

## 3、4 联动需求（已确认方向）

第 3、4 点必须联动设计，但本轮尚未开始实现。已确认的产品方向如下：

- **第 3 点：词库**：首批加入 CET6、雅思、托福。用户进入阅读页面前先选择词库；确定后，后续每一本小说都使用该词库，直到用户主动更换。词库选择应成为全局阅读偏好，并兼容现有 CET4 逻辑、书架、阅读进度、生词本和练习。
- **第 4 点：AI 小说**：使用本地模型，提前按不同素材/分类生成并存储小说，不在用户选择时现场生成。用户设置题材和篇幅，网页根据当前词库等限制条件筛选出可读范围，再由用户按兴趣选择具体小说。
- **词汇密度**：AI 小说使用现有低/中/高密度规则对应中英文比例；生成内容本身由 AI 控制质量，阅读器继续负责展示、练习、进度和生词本。
- **统一阅读体验**：AI 预存小说与用户本地上传小说共用同一套阅读器、练习、阅读进度和生词本。

## 3、4 仍需产品确认的实现细节

- “本地模型”是指在浏览器内运行（WebGPU/WASM），还是在本地电脑服务中预生成后再把成品资源打包进网页？网页端是否必须完全离线？
- 预存小说放在网页静态资源/PWA 包内，还是放在可更新的远程目录？预计首批分类、每类篇数、单篇长度和资源体积上限是多少？
- 词库选择是全局唯一且覆盖 CET4，还是 CET4、CET6、雅思、托福都作为可切换的独立词库？切换词库后，同一本小说的阅读进度和生词本是否继续共用，还是按词库隔离？
- 题材、篇幅之外，小说筛选是否还需要难度、章节数、目标词覆盖率、已读状态等条件？
- AI 小说的原始形态是中文正文 + 目标词映射，还是中英双语正文？低/中/高密度对应的具体比例是否继续采用当前约 1/3、2/3、100%？
- 预存小说是否允许用户删除/收藏/隐藏，是否需要版本更新提示和内容安全审核状态？

实施这些方向时，应继续兼容现有书架、阅读进度、生词和本地文件数据；在完成需求细化前，不把上述候选方向视为已进入开发或已承诺发布日期。

## 重要文件

- [候选策略](src/data/candidate-policy.ts)
- [批准候选池](src/data/approved-candidates.ts)
- [分词与上下文筛选](src/core/tokenizer.ts)
- [替换安全门](src/core/replacer.ts)
- [本地质量验收](tests/quality/local-novel-evaluation.test.ts)
- [候选审计脚本](scripts/audit-production-candidates.mjs)

## 本轮发布状态

阅读器升级已推送到 `main`。Cloudflare Pages 已完成部署；线上 `https://wordflow-dcx.pages.dev/` 使用 `index-C3onQJs0.js` / `index-BgPdif63.css`，首页返回 HTTP 200，横向/仿真分页采用固定页宽显式列布局，设置页统一承载自动翻页、背景和排版入口，`xuan-paper.webp` 可正常加载。手机端沿用同一套 PWA 构建，不分叉功能实现。
